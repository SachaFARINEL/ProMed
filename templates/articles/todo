1 - Problème de task quand une session est active. En créer une seule -> Show espace avec Authentification || espace ?
2 - Problème quand une session est active, on peut accéder aux deux espaces. Check isset session avec un rôle ? nom de table ? 
( SELECT * FROM `TABLES` WHERE  `TABLE_ROWS` = n° table concernée ) à essayer